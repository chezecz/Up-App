<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/welcome">

    <activity
        android:id="@+id/welcome"
        android:name="com.cheze.upapp.WelcomeApp"
        android:label="activity_welcome"
        tools:layout="@layout/activity_welcome" />

    <activity
        android:id="@+id/accounts_screen"
        android:name="com.cheze.upapp.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment android:id="@+id/balance_screen" />
    <fragment android:id="@+id/transactions_screen" />
</navigation>